# Example Vite env variables for frontend (prefix with VITE_). Copy to `.env` or `.env.local`.
VITE_API_BASE=http://localhost:5173
VITE_BACKEND_URL=http://127.0.0.1:8000

# Removed Firebase config (not used)
